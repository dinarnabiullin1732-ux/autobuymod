Step 19.8 — FullAuto 120 cycles + R stop inside auction

Что изменено:
1. FullAuto теперь запускается на 120 циклов вместо 50.
2. Ограничение cyclefullloop поднято до 120, чтобы .mab fullauto реально запускал 120 циклов.
3. Клавиша FullAuto теперь опрашивается не только в мире без GUI, но и при открытом серверном контейнере/аукционе GenericContainerScreen.
4. Поэтому если открыт /ah и FullAuto работает — нажатием R можно остановить цикл сразу.
5. В чате/GUI настроек клавиша не срабатывает, чтобы R не запускал/не останавливал автобай при вводе текста.

Не трогалось:
- smart reopen после покупки;
- storage relist;
- пропуск последней строки хранилища;
- sellcycle/sellloop;
- Ctrl+ЛКМ покупка через QUICK_MOVE.
