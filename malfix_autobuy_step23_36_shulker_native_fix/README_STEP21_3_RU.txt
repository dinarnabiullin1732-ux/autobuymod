Step 21.3 — fixed /ah search cheapest marker

Исправлено:
- подсветка больше не включается в главном /ah;
- /ah search теперь определяется по заголовку окна "Поиск | <предмет>", а не только по слову "Аукцион";
- скан цен замедлен до 700ms и кэшируется, чтобы меньше лагало;
- вместо большой рамки/заливки используется маленькая зелёная метка в углу + нижняя полоска.

Проверка:
/ah search порох

Если метки нет, нужно проверить точный текст строки цены в tooltip.
