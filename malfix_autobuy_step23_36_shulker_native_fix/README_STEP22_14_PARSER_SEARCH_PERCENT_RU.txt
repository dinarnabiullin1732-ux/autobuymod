Step 22.14 — Parser search percent fix

Что исправлено:
- Парсер теперь работает строго по схеме: предметы с тумблером «Парсить» -> /ah search <название> -> берёт самый дешёвый видимый лот -> ставит цену покупки/продажи по процентам из окна парсера.
- /ah search окна с названием вроде «Поиск | Порох» теперь распознаются как аукционные search-страницы для парсера.
- Обычный /ah не используется для парсинга, чтобы не брать случайные предметы с главного окна.
- Сначала пробуется точное совпадение по itemId/tag/name, затем fallback как в старом автобае: самый дешёвый priced lot на текущей странице /ah search. Это нужно для NBT/кастомных предметов.

Команды:
.mab parseall        - парсит только предметы, где включён тумблер «Парсить»
.mab parseall force  - парсит все предметы
.mab parse "Название" - парсит один предмет
.mab parser          - настройки процентов покупки/продажи

Сохранено:
- refresh 250ms
- scripts folder .minecraft/malfix_autobuy/scripts/
- shalk.js bridge
- spam-kick auto rejoin
- Anti-AFK
- tooltip цена за штуку
- зелёный квадратик в /ah search
- мод не спамит в игровой чат
